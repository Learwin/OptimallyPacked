package io.github.learwin.optimallypacked;

import net.neoforged.fml.ModContainer;
import net.neoforged.fml.common.Mod;

@Mod("optimallypacked")
public class OptimallyPacked {

    public OptimallyPacked(ModContainer container) {
        System.out.println(container.getModId() + " initialised!");
    }
}
